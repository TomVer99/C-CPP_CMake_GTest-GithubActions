name: Test_Run

on: [push]

env:
  BUILD_TYPE: Release

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    
    - uses: actions/checkout@v3
    
    - name: configure
      run: cmake -B ${{github.workspace}}/build -DCMAKE_BUILD_TYPE=${{env.BUILD_TYPE}}
    
    - name: Build
      run: cmake --build ${{github.workspace}}/build --config ${{env.BUILD_TYPE}}
    
    - name: Test
      working-directory: ${{github.workspace}}/build
      run: ctest -C ${{env.BUILD_TYPE}}



    # - name: Install gtest manually
    #   run: |
    #     sudo apt-get install libgtest-dev
    #     sudo apt-get install cmake
    #   # cd /usr/src/gtest
    #   # sudo cmake CMakeLists.txt
    #   # sudo make
    #   # sudo find . -name "*.a" -exec cp {} /usr/lib \;
    # - uses: actions/checkout@v3
    # - name: configure
    #   # run: mkdir build && cd build && cmake -DCMAKE_BUILD_TYPE=Debug -DCMAKE_CXX_FLAGS="-Werror" ..
    #   run: mkdir build && cd build
    # - name: make
    #   run: cd build && make
    # - name: Run Test
    #   run: /home/runner/work/Google_Test_Available/Google_Test_Available/build/test/Google_Test_Available.test